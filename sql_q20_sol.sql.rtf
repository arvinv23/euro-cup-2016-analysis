SELECT p.player_name, m.match_no\
FROM player_in_out io\
JOIN player_mast p ON io.player_id = p.player_id  \
JOIN match_mast m ON io.match_no = m.match_no\
WHERE io.in_out = 'I'\
  AND io.play_schedule = 'NT' \
  AND io.play_half = 1;}
