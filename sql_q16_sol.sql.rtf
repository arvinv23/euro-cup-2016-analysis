SELECT r.referee_name, v.venue_name, COUNT(m.match_no) AS num_matches\
FROM match_mast m\
JOIN referee_mast r ON m.referee_id = r.referee_id\
JOIN soccer_venue v ON m.venue_id = v.venue_id\
GROUP BY r.referee_name, v.venue_name;}
